import { Body, Controller, Get, Param, Patch, Post, Query, UseGuards } from '@nestjs/common';
import { IsArray, IsBoolean, IsEmail, IsEnum, IsOptional, IsString, Max, Min } from 'class-validator';
import { JwtAuthGuard, CurrentUser } from '../auth/jwt.strategy'; import { Roles, RolesGuard } from '../auth/roles.guard'; import { PartnerType, UserRole } from '../database/entities'; import { PartnersService } from './partners.service';
class CreatePartnerDto { @IsString() name!: string; @IsEnum(PartnerType) type!: PartnerType; @IsOptional() @IsString() logoUrl?: string; @IsOptional() @IsBoolean() showInCheckoutSummary?: boolean; @IsOptional() @IsEmail() email?: string; @IsOptional() @IsString() phone?: string; @IsOptional() @IsString() social?: string; @IsOptional() @IsString() promoCode?: string; @IsOptional() @Min(1) @Max(99) discountPercent?: number; @IsOptional() @IsArray() productIds?: string[]; @IsOptional() @IsString() note?: string; } class ActiveDto { @IsBoolean() isActive!: boolean; } class CheckoutSummaryDto { @IsBoolean() showInCheckoutSummary!: boolean; } class CommentDto { @IsString() body!: string; }
@UseGuards(JwtAuthGuard, RolesGuard) @Roles(UserRole.SUPER_ADMIN, UserRole.ADMIN) @Controller('admin/partners') export class PartnersController { constructor(private service: PartnersService) {} @Get() list(@Query('archived') archived?: string) { return this.service.list(archived === 'true'); } @Get('stats') stats() { return this.service.stats(); } @Get(':id') details(@Param('id') id: string) { return this.service.details(id); } @Post() create(@Body() body: CreatePartnerDto, @CurrentUser() user: { id: string }) { return this.service.create(body, user.id); } @Patch(':id/active') active(@Param('id') id: string, @Body() body: ActiveDto, @CurrentUser() user: { id: string }) { return this.service.setActive(id, body.isActive, user.id); } @Patch(':id/checkout-summary') checkoutSummary(@Param('id') id: string, @Body() body: CheckoutSummaryDto, @CurrentUser() user: { id: string }) { return this.service.setCheckoutSummary(id, body.showInCheckoutSummary, user.id); } @Post(':id/archive') archive(@Param('id') id: string, @CurrentUser() user: { id: string }) { return this.service.archive(id, user.id); } @Post(':id/comments') comment(@Param('id') id: string, @Body() body: CommentDto, @CurrentUser() user: { id: string }) { return this.service.addComment(id, body.body, user.id); } }
@Controller('partners') export class PublicPartnersController { constructor(private service: PartnersService) {} @Get('checkout') checkout() { return this.service.checkoutPartners(); } }
